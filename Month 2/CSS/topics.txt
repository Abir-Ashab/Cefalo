1) Block level elements (H1, H2, p...) --> jekhane declare kora hobe oi pura jayga niye nibe, pura line er jayga. Span is a non-block level elements. Check writing 2 line using h1 and 2 line with span.

We can set the default of displa to "inline", joto length er content oituk space e nibe